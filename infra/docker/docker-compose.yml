version: "3.9"
services:
  api:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ../../:/app
    command: sh -c "pip install -r requirements.txt && uvicorn apps.api-service.src.main:app --host 0.0.0.0 --port 8080"
    ports:
      - "8080:8080"
